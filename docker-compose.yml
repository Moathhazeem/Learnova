version: '3.8'

services:
  backend:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
    env_file:
      - ./server/.env

  frontend:
    build: ./src
    ports:
      - "80:80"
    depends_on:
      - backend
